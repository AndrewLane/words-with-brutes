namespace WordsWithBrutes.Enum
{
    /// <summary>
    /// Models the types of different tiles on the board that provide bonus points
    /// </summary>
    public enum SpecialTileType
    {
        TripleLetter,
        TripleWord,
        DoubleLetter,
        DoubleWord
    }
}
