namespace WordsWithBrutes.Enum
{
    /// <summary>
    /// Models the different directions a played word can be.  A single tile word is assumed to be horizontal.
    /// </summary>
    public enum WordDirection
    {
        Vertical,
        Horizontal
    }
}
