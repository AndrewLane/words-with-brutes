namespace WordsWithBrutes.Model
{
    /// <summary>
    /// Models the location of a given tile on the board, where (0,0) represents the top left corner of the board.
    /// </summary>
    public class TileLocation
    {
        public int X { get; set; }
        public int Y { get; set; }
    }
}
